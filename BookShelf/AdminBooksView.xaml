<Window x:Class="BookShelf.Views.AdminBooksView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookShelf.Views"
        xmlns:viewModels="clr-namespace:BookShelf.ViewModels"
        mc:Ignorable="d"
        Title="Admin - Manage Books" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance Type=viewModels:AdminBooksViewModel, IsDesignTimeCreatable=True}">

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="300"/>
            <!-- Book List -->
            <ColumnDefinition Width="Auto"/>
            <!-- Separator -->
            <ColumnDefinition Width="400"/>
            <!-- Edit Form -->
        </Grid.ColumnDefinitions>

        <!-- Column 0: List of Books -->
        <DockPanel Grid.Column="0">
            <TextBlock DockPanel.Dock="Top" Text="All Books" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <DataGrid ItemsSource="{Binding Books}"
                      SelectedItem="{Binding SelectedBook}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                    <DataGridTextColumn Header="Stock" Binding="{Binding StockQuantity}" Width="60"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>

        <!-- Column 1: Grid Splitter -->
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="LightGray"/>

        <!-- Column 2: Add/Edit Form -->
        <DockPanel Grid.Column="2" Margin="10,0,0,0">
            <TextBlock DockPanel.Dock="Top" Text="Book Details" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

            <!-- Action Buttons -->
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="New" Command="{Binding AddNewCommand}" Width="100" Margin="0,0,10,0"/>
                <Button Content="Save" Command="{Binding SaveCommand}" Width="100" Margin="0,0,10,0" FontWeight="Bold"/>
                <Button Content="Delete" Command="{Binding DeleteCommand}" Width="100" Background="IndianRed"/>
            </StackPanel>

            <!-- Form Fields -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel DataContext="{Binding BookInForm}">
                    <Label Content="Title"/>
                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Padding="2"/>

                    <Label Content="Author" Margin="0,10,0,0"/>
                    <ComboBox ItemsSource="{Binding DataContext.Authors, RelativeSource={RelativeSource AncestorType=Window}}"
                              DisplayMemberPath="FullName"
                              SelectedValuePath="AuthorID"
                              SelectedValue="{Binding AuthorID}"/>

                    <Label Content="Publisher" Margin="0,10,0,0"/>
                    <ComboBox ItemsSource="{Binding DataContext.Publishers, RelativeSource={RelativeSource AncestorType=Window}}"
                              DisplayMemberPath="Name"
                              SelectedValuePath="PublisherID"
                              SelectedValue="{Binding PublisherID}"/>

                    <Label Content="Category" Margin="0,10,0,0"/>
                    <ComboBox ItemsSource="{Binding DataContext.Categories, RelativeSource={RelativeSource AncestorType=Window}}"
                              DisplayMemberPath="Name"
                              SelectedValuePath="CategoryID"
                              SelectedValue="{Binding CategoryID}"/>

                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <Label Content="Price"/>
                            <TextBox Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}" Padding="2"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="5,0,5,0">
                            <Label Content="Stock"/>
                            <TextBox Text="{Binding StockQuantity, UpdateSourceTrigger=PropertyChanged}" Padding="2"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="5,0,0,0">
                            <Label Content="Year"/>
                            <TextBox Text="{Binding PublicationYear, UpdateSourceTrigger=PropertyChanged}" Padding="2"/>
                        </StackPanel>
                    </Grid>

                    <Label Content="Description" Margin="0,10,0,0"/>
                    <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
                             Height="80" TextWrapping="Wrap" AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto" Padding="2"/>
                </StackPanel>
            </ScrollViewer>
        </DockPanel>
    </Grid>
</Window>
