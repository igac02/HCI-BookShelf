<Window x:Class="BookShelf.Views.RegistrationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookShelf.Views"
        xmlns:viewModels="clr-namespace:BookShelf.ViewModels"
        mc:Ignorable="d"
        Title="Register New Account" Height="550" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        d:DataContext="{d:DesignInstance Type=viewModels:RegistrationViewModel, IsDesignTimeCreatable=True}">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Create Your Account" 
                   FontSize="28" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,20,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="25,0">
                <Label Content="First Name"/>
                <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Padding="5" FontSize="14"/>

                <Label Content="Last Name" Margin="0,10,0,0"/>
                <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Padding="5" FontSize="14"/>

                <Label Content="Email Address" Margin="0,10,0,0"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Padding="5" FontSize="14"/>

                <Label Content="Password" Margin="0,10,0,0"/>
                <PasswordBox x:Name="PasswordBox1" PasswordChanged="PasswordBox_PasswordChanged" Padding="5" FontSize="14"/>

                <Label Content="Confirm Password" Margin="0,10,0,0"/>
                <PasswordBox x:Name="PasswordBox2" PasswordChanged="ConfirmPasswordBox_PasswordChanged" Padding="5" FontSize="14"/>

                <Button Content="Register" 
                        Command="{Binding RegisterCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        FontSize="18" FontWeight="Bold"
                        Margin="0,30,0,0" Padding="10"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
