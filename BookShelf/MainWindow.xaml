<Window x:Class="BookShelf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:BookShelf.ViewModels"
        xmlns:converters="clr-namespace:BookShelf.Converters"
        mc:Ignorable="d"
        Title="BookShelf - Main" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel, IsDesignTimeCreatable=True}">
    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Bar with Welcome Message and Action Buttons -->
        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="{Binding LoggedInUser.FirstName, StringFormat='Welcome, {0}'}" FontSize="18" VerticalAlignment="Center"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="View Cart" Command="{Binding ViewCartCommand}" Margin="5" Padding="10,5"/>

                <StackPanel Orientation="Horizontal" Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Button Content="Manage Books" Command="{Binding OpenAdminBooksCommand}" Margin="5" Padding="10,5"/>
                    <Button Content="Manage Users" Command="{Binding OpenAdminUsersCommand}" Margin="5" Padding="10,5"/>
                    <Button Content="View Orders" Command="{Binding OpenAdminOrdersCommand}" Margin="5" Padding="10,5"/>
                </StackPanel>

                <Button Content="Logout" Command="{Binding LogoutCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Margin="20,5,5,5" Padding="10,5" Background="LightCoral"/>
            </StackPanel>
        </DockPanel>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <!-- Filters Panel -->
                <ColumnDefinition Width="*"/>
                <!-- Main Content -->
            </Grid.ColumnDefinitions>

            <!-- Left Panel for Filtering -->
            <Border Grid.Column="0" BorderBrush="LightGray" BorderThickness="0,0,1,0" Padding="10">
                <StackPanel>
                    <TextBlock Text="Search &amp; Filter" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <Label Content="Search by Title or Author"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}" Padding="5"/>
                    <Label Content="Category" Margin="0,15,0,0"/>
                    <ComboBox ItemsSource="{Binding Categories}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedCategory}" Padding="5"/>
                    <Label Content="Author" Margin="0,15,0,0"/>
                    <ComboBox ItemsSource="{Binding Authors}" DisplayMemberPath="FullName" SelectedItem="{Binding SelectedAuthor}" Padding="5"/>
                    <Button Content="Clear Filters" Command="{Binding ClearFiltersCommand}" Margin="0,20,0,0" Padding="5"/>
                </StackPanel>
            </Border>

            <!-- Right Panel for Book List -->
            <Grid Grid.Column="1" Margin="10,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <DataGrid Grid.Row="0" ItemsSource="{Binding Books}" SelectedItem="{Binding SelectedBook}" AutoGenerateColumns="False" IsReadOnly="True" FontSize="14">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="Author" Binding="{Binding Author.FullName}" Width="200"/>
                        <DataGridTextColumn Header="Category" Binding="{Binding Category.Name}" Width="150"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat={}{0:C}}" Width="100"/>
                        <DataGridTextColumn Header="In Stock" Binding="{Binding StockQuantity}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
                <DockPanel Grid.Row="1" Margin="0,10,0,0">
                    <StatusBar DockPanel.Dock="Left">
                        <StatusBarItem>
                            <TextBlock Text="{Binding Books.Count, StringFormat={}{0} books found}"/>
                        </StatusBarItem>
                    </StatusBar>
                    <Button DockPanel.Dock="Right" Content="Add to Cart" Command="{Binding AddToCartCommand}" Width="120" Padding="5" FontWeight="Bold"/>
                </DockPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
