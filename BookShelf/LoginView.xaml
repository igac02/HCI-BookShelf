<Window x:Class="BookShelf.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookShelf.Views"
        xmlns:viewModels="clr-namespace:BookShelf.ViewModels"
        xmlns:converters="clr-namespace:BookShelf.Converters"
        mc:Ignorable="d"
        Title="Login - BookShelf" Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        d:DataContext="{d:DesignInstance Type=viewModels:LoginViewModel, IsDesignTimeCreatable=True}">

    <Window.Resources>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Welcome to BookShelf" 
                   FontSize="28" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,40,0,20"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="40,0">

            <Label Content="Email Address" />
            <TextBox x:Name="EmailTextBox" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" FontSize="16" Padding="5"/>

            <Label Content="Password" Margin="0,10,0,0"/>
            <PasswordBox x:Name="PasswordBox" FontSize="16" Padding="5" PasswordChanged="PasswordBox_PasswordChanged"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,0" TextWrapping="Wrap" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

            <Button x:Name="LoginButton" Content="Login" Command="{Binding LoginCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" FontSize="18" FontWeight="Bold" Margin="0,30,0,0" Padding="10"/>

            <!-- Link za otvaranje prozora za registraciju -->
            <TextBlock HorizontalAlignment="Center" Margin="0,15,0,0">
                <Run Text="Don't have an account?"/>
                <Hyperlink Command="{Binding OpenRegisterCommand}">
                    <Run Text="Register here"/>
                </Hyperlink>
            </TextBlock>

        </StackPanel>

        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="Please enter your credentials to continue."/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
